import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Smart Bookmark — Save & Organize Your Bookmarks",
  description:
    "A real-time bookmark manager. Save, organize, and access your bookmarks from anywhere. Sign in with Google to get started.",
  keywords: ["bookmark", "manager", "real-time", "save", "organize"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
